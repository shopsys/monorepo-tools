# disable logging in tests
monolog:
    handlers:
        main:
            type: "null"
        nested:
            type: "null"
        cron:
            type: "null"
        slow:
            type: "null"
        cron_console_output:
            type: "null"
        console_output:
            type: "null"
