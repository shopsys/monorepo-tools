# disable logging in tests
monolog:
    handlers:
        main:
            type: "null"
            excluded_404s: false
        nested:
            type: "null"
        cron:
            type: "null"
        slow:
            type: "null"
        cron_console_output:
            type: "null"
        console_output:
            type: "null"
