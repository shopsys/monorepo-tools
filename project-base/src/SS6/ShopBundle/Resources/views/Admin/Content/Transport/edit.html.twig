{% extends '@SS6Shop/Admin/Content/Transport/detail.html.twig' %}

{% block title %}- {{ 'Editace dopravy'|trans }} - {{ transportDetail.transport.name }}{% endblock %}
{% block h1 %}{{ 'Editace dopravy'|trans }} - {{ transportDetail.transport.name }}{% endblock %}

{% block transport_base_prices %}
	<div id="transport_prices">
		<table class="table">
			<thead>
				<tr>
					<th>{{ inputPriceLabel() }}</th>
					<th>{{ 'Cena bez DPH'|trans }}</th>
					<th>{{ 'Cena s DPH'|trans }}</th>
				</tr>
			</thead>
			<tbody>
				{% for currencyId, currency in currencies %}
					<tr>
						<td>
							{{ form_row(
								form.prices[currencyId],
								{label: 'Základní cena v %currencyCode%'|trans({'%currencyCode%': currency.code}), symbolAfterInput:currencySymbolByCurrencyId(currencyId) }
							) }}
						</td>
						<td>{{ transportDetail.basePrices[currencyId].priceWithoutVat|priceWithCurrencyByCurrencyId(currencyId) }}</td>
						<td>{{ transportDetail.basePrices[currencyId].priceWithVat|priceWithCurrencyByCurrencyId(currencyId) }}</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}

{% block image %}
	<div class="form-line">
		<label class="form-line__label">
			{{ 'Obrázek'|trans }}:
		</label>
		<div class="form-line__line">
			{{ image(transportDetail.transport, { size: 'original', height: 100 }) }}
		</div>
	</div>
{% endblock %}