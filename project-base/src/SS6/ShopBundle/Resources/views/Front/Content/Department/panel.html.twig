<ul class="list-menu">
	{% for department in departments %}
		<li class="list-menu__item">
			<a href="{{ url('front_product_department', { departmentId: department.id }) }}">{{ department.name }}</a>
		</li>
	{% endfor %}
</ul>
