{% block begin %}
<?xml version="1.0" encoding="utf-8"?>
<SHOP>
{% endblock %}

{% block item %}
    <SHOPITEM>
        <ITEM_ID>{{ item.itemId }}</ITEM_ID>
        {% if item.groupId is not null -%}
            <ITEMGROUP_ID>{{ item.groupId }}</ITEMGROUP_ID>
        {% endif -%}
        <PRODUCTNAME>{{ item.productName }}</PRODUCTNAME>
        <DESCRIPTION>{{ item.description|striptags|replace("\n", ' ')|trim }}</DESCRIPTION>
        <URL>{{ item.url }}</URL>
        {% if item.imgUrl -%}
            <IMGURL>{{ item.imgUrl }}</IMGURL>
        {% endif -%}
        <PRICE_VAT>{{ item.priceVat|number_format(2, ',', '') }}</PRICE_VAT>
        {% if item.ean -%}
            <EAN>{{ item.ean }}</EAN>
        {% endif -%}
        {% if item.deliveryDate is not null -%}
            <DELIVERY_DATE>{{ item.deliveryDate }}</DELIVERY_DATE>
        {% endif -%}
        {% if item.manufacturer is not null -%}
            <MANUFACTURER>{{ item.manufacturer }}</MANUFACTURER>
        {% endif -%}
        {% if item.categoryText is not null -%}
            <CATEGORYTEXT>{{ item.categoryText }}</CATEGORYTEXT>
        {% endif -%}
        {% for paramName, paramVal in item.parametersByName -%}
            <PARAM>
                <PARAM_NAME>{{ paramName }}</PARAM_NAME>
                <VAL>{{ paramVal }}</VAL>
            </PARAM>
        {% endfor -%}
        {% if item.cpc is not null -%}
            <HEUREKA_CPC>{{ item.cpc|number_format(2, ',', '') }}</HEUREKA_CPC>
        {% endif -%}
    </SHOPITEM>
{% endblock %}

{% block end %}
</SHOP>
{% endblock %}
