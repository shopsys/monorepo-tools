{{ form_start(form, { attr: { 'data-on-submit': 'ajaxSubmit', 'data-success': success ? 'true' : 'false' }}) }}
    <div class="footer__newsletter">
        <div class="footer__newsletter__text">
            <p>
                {{ 'Do you want to receive the newest information about our shop? Subscribe to our e-mail newsletter.'|trans }}
            </p>
        </div>
        <div class="footer__newsletter__form">
            <div class="footer__newsletter__form__wrap">
                <div class="footer__newsletter__form__in">
                    {{ form_widget(form.email, { attr: {placeholder: 'Enter e-mail'|trans, class: 'footer__newsletter__input'} }) }}
                    {{ form_errors(form.email) }}
                </div>
                {{ form_widget(form.send, { label: 'Log in'|trans, attr: { class: "footer__newsletter__form__btn" }}) }}
            </div>
            {{ form_errors(form, { errors_attr: { class : 'in-message--alert' } }) }}
            <div class="form-choice">
                <div class="form-choice__input">
                    {{ form_widget(form.privacyPolicyAgreement) }}
                </div>
                <div class="form-choice__label">
                    <label for="{{ form.privacyPolicyAgreement.vars.id }}">
                        {{ 'Please confirm privacy policy agreement.'|trans }}
                    </label>
                    <span class="js-validation-errors-list js-validation-error-list-{{ form.privacyPolicyAgreement.vars.id }} form-error form-error--choice display-none"><span class="form-error__icon"><i class="svg svg-warning"></i></span><ul class="form-error__list"></ul></span>
                </div>
            </div>
        </div>
    </div>
{{ form_end(form) }}
{{ init_js_validation(form, false) }}
